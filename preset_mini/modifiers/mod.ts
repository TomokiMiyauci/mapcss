import { breakpoints } from "./breakpoint.ts";
import { colorSchemes } from "./color_scheme.ts";
import { hovers } from "./hover.ts";

export const nestedModifiers = [
  breakpoints,
  colorSchemes,
  hovers,
];

export { breakpoints, colorSchemes, hovers };
